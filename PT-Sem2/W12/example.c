#include <stdio.h>

/*  
    A is the solution vector and k is the step in the solution vector

    def Back(A, k) 
        if A =  (a1,a2,...,ak) is a solution 
            process_solution
        else
            generate valid ak -> go through all possible values of ak and validate them
            Back(A, k + 1)
*/

int main() {
    return 0;
}